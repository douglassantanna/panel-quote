export interface Customer {
  id: number;
  name: string;
  email: string;
  phoneNumber: string;
  birthDate: Date;
}
export interface NewCustomer {
  name: string;
  email: string;
  phoneNumber: string;
  birthDate: Date;
}
